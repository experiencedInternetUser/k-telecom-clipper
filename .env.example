# Backend API URL
# For development: http://localhost:5000 or http://host.docker.internal:5000
# For production: http://your-backend-server:5000
VITE_API_URL=http://155.212.146.240:5000

# Backend API URL for Vite proxy (development only)
# This is used by Vite dev server proxy to forward /api requests
# Should match VITE_API_URL in development, not used in production builds
VITE_PROXY_TARGET=http://155.212.146.240:5000
